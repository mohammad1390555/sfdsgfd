# Security Notes

- Do not paste active GitHub tokens into chat, issues, or repository files.
- If a token was shared accidentally, revoke it immediately and create a new one.
- Use `publish.sh` to enter credentials locally on your own machine without saving them in the repository.
- Prefer GitHub CLI (`gh auth login`) or SSH keys when possible.
